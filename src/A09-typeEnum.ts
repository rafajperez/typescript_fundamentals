/* Enum is a data structure that allows you to define a set of named constant values. 
it is a powerful tool for improving code organization and readability.Enum is useful in scenarios 
where data changes are not constant, 
such as: Gender registration, Days of the week, Social network registration.
 */

enum Colors {
  Red,
  blue,
  yellow,
}
console.log(Colors);

console.log(Colors.Red);
